{
  "name": "product-engine-microgame-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:stress": "vitest run tests/stress",
    "test:coverage": "vitest run --coverage",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "e2e": "playwright test",
    "factory:status": "node factory/gate-runner.mjs factory/projects/critter-flip.json",
    "factory:json": "node factory/gate-runner.mjs factory/projects/critter-flip.json --json",
    "factory:validate": "node factory/gate-runner.mjs factory/projects/critter-flip.json --validate-only",
    "verify:core": "npm run factory:validate && npm run typecheck && npm run test:coverage && npm run test:stress && npm run build"
  },
  "devDependencies": {
    "@axe-core/playwright": "latest",
    "@playwright/test": "latest",
    "@types/node": "latest",
    "@vitest/coverage-v8": "latest",
    "fast-check": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
